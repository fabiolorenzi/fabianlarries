import React from "react";

function NextEvents() {
    return (
        <div className="nextEvents">
            <main className="nextEv container">
                <div>
                    <h1>Due to the covid19, currently there are no events in program.</h1>
                </div>
            </main>
        </div>
    );
};

export default NextEvents;